# Environment variables needed for blaze local development.
# To use, copy this file to a new file named ".env" and fill in your private keys and settings.
# Your actual .env file should NOT be committed.

# -----------------------------------------------------------------------------
# Required for local app runtime
# -----------------------------------------------------------------------------
# Preferred PostgreSQL connection string used by Blaze app/runtime.
DATABASE_URL=
# Optional fallback (used only when DATABASE_URL is empty).
POSTGRES_URL=

# -----------------------------------------------------------------------------
# Multi-tenant and auth runtime flags
# -----------------------------------------------------------------------------
# Multi-tenant mode:
# - shadow: scoped + legacy aliases enabled
# - enforce: scoped-only; legacy unscoped endpoints return 410
MULTITENANT_MODE=shadow
# Enable WebSocket chat streaming protocol handlers (SSE is always available).
WS_STREAMING_ENABLED=true
# Legacy /api/ipc/invoke bridge (target: false after migration window).
IPC_LEGACY_ENABLED=true

# Auth mode:
# In development, missing Bearer JWT can be replaced by synthetic dev identity.
AUTH_DEV_BYPASS_ENABLED=true
# Optional strict JWT claim checks:
AUTH_JWT_ISSUER=
AUTH_JWT_AUDIENCE=
# Optional HS256 signature verification (for local/dev JWTs):
AUTH_JWT_HS256_SECRET=
# If true, reject tokens when signature cannot be verified by configured method.
AUTH_JWT_STRICT_SIGNATURE=false

# Optional dev identity headers fallback values (used by browser HTTP client):
VITE_BLAZE_DEV_USER_SUB=
VITE_BLAZE_DEV_USER_EMAIL=
VITE_BLAZE_DEV_USER_NAME=
# Optional explicit tenant selection in HTTP client (defaults: org=me, workspace=me):
VITE_BLAZE_TENANT_ORG_ID=me
VITE_BLAZE_TENANT_WORKSPACE_ID=me
# Optional Bearer token passed by browser HTTP client:
VITE_BLAZE_AUTH_TOKEN=

# -----------------------------------------------------------------------------
# AI provider API keys (optional; required depends on selected provider)
# -----------------------------------------------------------------------------
# OpenRouter (also used by AUTO cloud fallback chain):
OPENROUTER_API_KEY=
# OpenAI provider:
OPENAI_API_KEY=
# Anthropic provider:
ANTHROPIC_API_KEY=
# Google Gemini provider:
GEMINI_API_KEY=
# xAI provider:
XAI_API_KEY=
# Azure OpenAI provider (both values are required together):
AZURE_API_KEY=
AZURE_RESOURCE_NAME=
# AWS Bedrock provider:
AWS_BEARER_TOKEN_BEDROCK=
AWS_REGION=us-east-1

# Local AI Model Configuration (Optional)
# Set these if you are running local AI models like Ollama or LM Studio.
# Default for Ollama is http://127.0.0.1:11434
OLLAMA_HOST=
# Default for LM Studio is http://localhost:1234
LM_STUDIO_BASE_URL=

# -----------------------------------------------------------------------------
# AUTO model priority (selectedModel: provider=auto, name=auto)
# -----------------------------------------------------------------------------
# 1) LM Studio loaded LLM from LM_STUDIO_BASE_URL/api/v0/models (if available)
# 2) openrouter/deepseek/deepseek-r1-0528:free
# 3) openrouter/mistralai/mistral-small-3.2-24b-instruct:free
# 4) openrouter/openai/gpt-4o-mini
#
# Notes:
# - Steps 2-4 require OPENROUTER_API_KEY.
# - If neither LM Studio nor cloud candidates are available, chat will fail.

# GitHub Integration (Optional)
# Needed for features that interact with GitHub repositories.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_TOKEN=


# Apple Notarization (macOS Build Only)
# Only required if you are building and signing a release version for macOS.
APPLE_ID=
APPLE_PASSWORD=
APPLE_TEAM_ID=
SM_CODE_SIGNING_CERT_SHA1=


# Development & Testing Variables (Advanced)
# These are typically not needed for standard contribution.
# NODE_ENV=development
# E2E_TEST_BUILD=
# CI=
# BLAZE_ENGINE_URL=
# BLAZE_GATEWAY_URL=
